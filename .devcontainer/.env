# Enable or disable database services by editing this line.
# Available profiles: postgres, trino, starrocks
# Remove a profile name to disable that service.
COMPOSE_PROFILES=postgres,trino,starrocks
